$(window).on("load", function() {
    // Quando tudo, incluindo imagens, for carregado, seu código aqui será executado
    $('#carouselExample').carousel(); // Inicializa o carrossel após tudo ser carregado
});
